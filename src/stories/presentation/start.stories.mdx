import {Meta, Canvas, Story} from "@storybook/addon-docs";
import {Button} from "../Button";




<Meta title="Example/presentation/Introduction" />

export const Template = (args) => <Button {...args} />;

<style>
  {`
    .sbdocs-h1,sbdocs-h2,.sbdocs-h3,.sbdocs-h4,.sbdocs-h5 {
        color : #ff4785;
        margin : 10px 0 !important;
    }

    .sbdocs-blockquote{
        border-color: #1EA7FD;
    }
  `}

</style>

# Что такое Storybook ?

### Вот что такое история :)

<Canvas>
  <Story
    name="Default"
    args={{
      size: 'large',
      label: 'Default',
    }}>
    {Template.bind({})}
  </Story>
   <Story
      name="Primary"
      args={{
          primary: true,
          label: 'Primary',
      }}>
      {Template.bind({})}
    </Story>
</Canvas>


```
export default {
  title: 'Example/Button',
  component: Button,
  argTypes: {
    backgroundColor: { control: 'color' },
  },
}
const Template: ComponentStory<typeof Button> = (args) => <Button {...args} />;

export const Primary = Template.bind({});
Primary.args = {
  primary: true,
  label: 'Button',
};

```



### А сегодня мы узнаем как делать простые истории а так же :

> [как настраивать аргументы историй](https://storybook.js.org/docs/react/writing-stories/args)

> [как писать документацию](https://storybook.js.org/docs/react/writing-docs/introduction)

> [какие тесты компонент позволяет делать storybook](https://storybook.js.org/docs/react/writing-tests/introduction)

> [интеренсные плагины](https://storybook.js.org/docs/react/addons/introduction)
